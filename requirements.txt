# 暴涨做空策略系统依赖 / Pump Short Strategy System Dependencies
# =================================================================
#
# 本文件列出了运行暴涨做空策略系统所需的所有Python包及其版本
# This file lists all Python packages and their versions required to run 
# the pump short strategy system
#
# 安装方法 / Installation:
# pip install -r requirements.txt

# 核心数据处理库 / Core Data Processing Libraries
pandas>=1.5.0                 # 数据处理和分析 / Data processing and analysis
numpy>=1.21.0                 # 数值计算 / Numerical computing
scipy>=1.9.0                  # 科学计算 / Scientific computing

# 配置文件处理 / Configuration File Processing
PyYAML>=6.0                   # YAML文件解析 / YAML file parsing

# 日志和调试 / Logging and Debugging
loguru>=0.6.0                 # 高级日志记录（可选）/ Advanced logging (optional)

# 时间处理 / Time Processing
python-dateutil>=2.8.0        # 日期时间处理 / Date and time processing
pytz>=2022.1                  # 时区处理 / Timezone handling

# 数学和统计 / Mathematics and Statistics
scikit-learn>=1.1.0           # 机器学习库（可选用于高级分析）/ ML library (optional for advanced analysis)
statsmodels>=0.13.0           # 统计模型（可选）/ Statistical models (optional)

# 文件和数据格式 / File and Data Formats
openpyxl>=3.0.0               # Excel文件处理（可选）/ Excel file processing (optional)
xlsxwriter>=3.0.0             # Excel写入（可选）/ Excel writing (optional)

# 可视化库（可选）/ Visualization Libraries (Optional)
# matplotlib>=3.5.0           # 基础绘图 / Basic plotting
# seaborn>=0.11.0             # 统计可视化 / Statistical visualization
# plotly>=5.0.0               # 交互式图表 / Interactive charts

# 性能优化（可选）/ Performance Optimization (Optional)
# numba>=0.56.0               # JIT编译器 / JIT compiler
# cython>=0.29.0              # C扩展 / C extensions

# 网络和API（未来扩展用）/ Network and API (For Future Extensions)
# requests>=2.28.0            # HTTP库 / HTTP library
# websocket-client>=1.3.0     # WebSocket客户端 / WebSocket client
# ccxt>=2.0.0                 # 加密货币交易所API / Crypto exchange APIs

# 数据库（未来扩展用）/ Database (For Future Extensions)
# sqlalchemy>=1.4.0          # SQL数据库ORM / SQL database ORM
# sqlite3                     # SQLite数据库（Python内置）/ SQLite database (built-in)

# 测试框架（开发用）/ Testing Framework (For Development)
# pytest>=7.0.0              # 测试框架 / Testing framework
# pytest-cov>=3.0.0          # 测试覆盖率 / Test coverage

# 代码质量（开发用）/ Code Quality (For Development)
# black>=22.0.0               # 代码格式化 / Code formatting
# flake8>=4.0.0               # 代码检查 / Code linting
# mypy>=0.950                 # 类型检查 / Type checking

# 文档生成（开发用）/ Documentation Generation (For Development)
# sphinx>=4.5.0              # 文档生成 / Documentation generation
# sphinx-rtd-theme>=1.0.0    # 文档主题 / Documentation theme

# 注意事项 / Notes:
# 1. 版本号使用 >= 以确保兼容性 / Version numbers use >= to ensure compatibility
# 2. 可选依赖已注释，按需取消注释安装 / Optional dependencies are commented, uncomment as needed
# 3. 如果遇到版本冲突，可以尝试降低版本要求 / If version conflicts occur, try lowering version requirements
# 4. 建议使用虚拟环境安装依赖 / Recommend using virtual environment for installation

# Python版本要求 / Python Version Requirements:
# Python >= 3.8 推荐 Python 3.9+ / Python >= 3.8, recommend Python 3.9+ 